export interface BenefitCard {
  icon: string;
  title: string;
  description: string;
  backgroundColor?: string;
  button?: boolean;
  buttonName?: string;
  buttonLink?: string;
  comingSoon?: boolean;
}
